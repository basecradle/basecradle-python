# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/

# Environments & tooling
.venv/
.env
.env.*
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/

# Scratch files. Added after a stray `.api.yaml.tmp` was left in the clone (#116);
# that came from a manual spec fetch, not from the suite — the drift-guard holds
# the spec in memory and writes nothing.
*.tmp

# OS
.DS_Store
